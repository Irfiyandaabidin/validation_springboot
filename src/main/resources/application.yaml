server:
  port: 9090

person:
  name:
    first: irfiyanda
    last: abidin

spring:
  application:
    name: crud-service
  datasource:
    url: jdbc:postgresql://localhost:5432/crud?currentSchema=public
    username: postgres
    password: Amikom123
    driver-class-name: org.postgresql.Driver
    maximumPoolSize: 10

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update
    show-sql: true
    generate-ddl: true

